# The completed response archive retains every raw byte; the local raw file stays.
artifacts/confirmation/responses.jsonl
# Ephemeral uncertainty journal, not a completed experimental artifact.
artifacts/confirmation/pending_call.json
