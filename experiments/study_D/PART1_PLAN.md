# Study D Part 1 — development exploration

Status: authorized development exploration; no confirmatory outcome run.

The user's 2026-09-05 instruction delegates the end-to-end process, including resolving the open draft choices before registration. This supersedes the draft's earlier revision-only authorization. Binding gates, blind scoring, and design-before-implementation remain in force. No production adoption is authorized.

## Scope and order

Freeze the incoming repository at `5ebda1ef` in a separate control worktree. Copy Revision 2 as a historical design artifact. Commit this plan before executing exploration code. Explore the existing public CC-007 read path and its prior replay population first. Do not run or inspect new confirmatory reader outcomes. Do not change carried package code.

First deliverables: input inventory with hashes/counts, runtime availability, independent frozen control replay, public read-path behavior distributions and failure cases, then an explicit decision record resolving the character-versus-token budget mismatch. If an infrastructure prerequisite fails, characterize and repair it within the registered semantics, or record the exact unavailable prerequisite without a mechanism verdict.

Subsequent development corpus, route prototype and reader calibration require a separately committed concrete Part 1 extension defining their populations and call limits before execution. This initial plan authorizes inventory and existing-component replay only, not unconstrained corpus or parameter sweeps.

## Preflight

Part 1 behavioral question: Does the public baseline always render up to 32 recent episodes outside its 32,000-character CC80 retrieval block, independently of query relevance, and can its total serialized context exceed the named retrieval budget?

Name checks: verify recency, semantic ranking, budget and total context against actual emitted bytes, source identity and order. Report distributions and saturated/empty/singleton states. The public function's pure-call behavior and any store-state dependency must be distinguished.

Part 2 for this bounded development stage:

- PF1: Before replay, hash and count every consumed frozen input/cache, baseline module and runtime file; missing resources halt that replay.
- PF2: Execute the existing parity harness from frozen code and direct public read-path fixtures; cite exact output identities and payload hashes.
- PF3: Plan commit precedes execution; no new study mechanism or confirmatory outcomes are authorized here.
- PF4: Parity's equality branch is possible because expected artifacts were generated by the carried implementation. Verify nonempty join counts; an absent dataset cannot count as mismatch evidence.
- PF5: Compare ordered content and payload digests. Generated IDs alone are not an equality certificate.
- PF6: Existing CC-007 frozen 4,355-group parity test is the reproduction target. Redirect output to this study without overwriting prior artifacts.
- PF7: Check stateless public composition with repeated identical calls and changed latest source at maximum fixture scale; do not infer anything about untested historical runners.
- PF8: Real replay plus direct fixtures characterize static behavior, not reader endurance. No 120-turn reader run begins here.
- PF9: Identical replay can reproduce a wrong named behavior. Report naming and budget behavior separately; no gold, reader or success claim follows from byte identity.
- PF10: No availability verdict or adoption; the later registered reader comparison remains mandatory.

All reports identify executed commands, source commit, module paths and input hashes. Missing or failed checks are never labeled PASS. Read-only inspection to establish PF1 is permitted before other exploration executes. Resource hashing and independent input checks may run concurrently; model calls remain serial and use no speculative decoding.
