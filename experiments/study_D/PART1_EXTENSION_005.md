# Study D Part 1 extension 005 — value opacity and live instrument check

The substantive-history instrument creates T1 headroom: development complete evidence C0 1/4, C1 2/4; all other answerable types are 4/4. No reader result has been opened. This supports testing this narrow ordering contrast, not claiming a general temporal or multi-hop deficit.

Before confirmatory lock, replace ordinal-bearing `RV-seed-turn` code values with opaque deterministic digest-derived codes. The old format exposed a regular answer pattern and could allow extrapolation without the exact assertion. This is a measurement-confound repair, not another structural corpus expansion: source positions, queries, revision counts, route and budgets are unchanged. Keep both prior development artifacts. New artifacts live in `development_opaque/`. Generator hashing uses a fixed value namespace recorded in code; values are not semantic hints. Gold and sources receive the same replacement in the corpus-authoring stage only. Require uniqueness and no exposed seed/turn codes. Re-run the 28 development source/availability checks with the same solo embedding shape before lock.

Authorize 35 serial development reader calls after opaque prompts and token counts are sealed: cycle through the 28 probe identities in their existing session/type order, use C0 for even call indices and C1 for odd, seed 5005. Persist every response immediately. Use the frozen HH-001 closed-think prompt and existing GPU sampling settings, but an 8,192-token common output allowance prospectively, before any long-prompt call. This is a 35-call snapshot-instrument ablation with 140-source histories, not a 35-turn evolving conversation. It checks source-scale prompt compatibility and completeness; it cannot certify conversational feedback, which remains out of scope. Do not score these answers or use them to tune route/prompt parameters. No confirmatory facts or cases are included.

Require all complete with natural stop and final text, no hidden context truncation, and exact serialized prompt plus output allowance below 65,536. Any failure stops the reader instrument before registration; the full outcome study remains unrun. No capped answer is extended. Verify a duplicate of the maximum-token prompt twice at seed 5005 after the 35 calls (37 total maximum) for long-prefix byte identity. Do not use ordinary text-only extraction if a reasoning block is left open.

PF1-PF10 carry with opacity, input seals, 35-call schedule, cap checks and duplicate-prefix evidence added. A single case still has only one independent source session regardless of repeated calls. The 35-call ablation is appropriate to this stateless frozen-history design and must not be reported as a 120-turn live run. Reader outcomes remain sealed/unscored; runtime code may inspect only completeness and identity fields. This extension must be committed before implementation and calls.
