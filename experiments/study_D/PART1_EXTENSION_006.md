# Study D Part 1 extension 006 — prefix reuse feasibility

Long prompts cost about 4.5 seconds each even on GPU. Before selecting a confirmatory execution schedule, test whether server prefix reuse preserves seeded bytes. This is an execution optimization only; no route, corpus, prompt, reader cap or outcome criterion changes.

After extension 005 completes successfully, choose its maximum-token, minimum-token and first scheduled prompts by sealed metadata, not outputs. For each, make one cache-disabled call followed by one cache-enabled call, all at seed 5005 with the same registered GPU sampler and 8,192 ceiling. Six calls maximum. Persist raw responses separately in `cache_probe/`. Require natural complete final text and byte identity within each pair. No answer scoring. If any pair differs, prefix reuse is disallowed for confirmation; the cache-disabled schedule remains the default. Report the failure and do not tune the check. If all match, a completed registration may prospectively adopt cache-enabled calls with consecutive seed replicates per identical prompt, while retaining a confirmatory prefix replay gate and recording all slot state settings.

PF1: inherited file/prompt hashes plus extension-005 PASS. PF2: inspect requested and returned runtime settings, exact bytes and timings. PF3: this commit and extension-005 PASS precede six calls. PF4: three nonempty distinct metadata-selected surfaces, both branches possible. PF5: content-hash comparisons. PF6: the cold call is the paired execution reference, not a correctness oracle. PF7: this finite check cannot guarantee invariance across every cache/server state; record the residual. PF8: include the maximum development prompt; no claim beyond snapshot execution. PF9: identity says nothing about correctness or independence of seeds. PF10: full study still required. No additional calls after failure without a new explicit plan.
