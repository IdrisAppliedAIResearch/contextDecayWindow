# Amendment 002: stop generation and gauge the completed paired subset

The user explicitly requested stopping the full-corpus experiment and scoring the already completed paired subset: "Yes, we can gauge the signal." Generation is stopped at that instruction, without inspecting correctness. Preserve every request/response and any interrupted request; never retry or fill an unfinished pair.

Select all question occurrences for which both frozen C0 and C1 prompt hashes have valid persisted EOS answers at the stop. Reconstruct aliases from the already frozen prompt manifest. Selection uses completeness only, never answer content, gold, evidence, or agreement. Preserve unpaired, missing, malformed and interrupted cases in an exclusion inventory. Exact duplicate prompts retain the original reuse policy. Freeze and commit this subset and raw hashes before any judging.

This is an opportunistically stopped, nonrandom schedule subset across the previously exposed conversations. The full 1,540-primary comparison is NOT COMPLETED. No original full-population WORKS disposition can be earned. Report an exploratory paired effect, gains/losses, per-category and per-conversation results, and the original cluster-bootstrap interval as descriptive uncertainty, not a representative-sample or confirmatory guarantee. Any reference to the original 1pp/2pp effect sizes is descriptive only. Do not exclude a completed category after seeing its scores.

Keep the original frozen retrieval, captions, chronology, reader answers, scorer prompt/runtime/settings, three blind votes plus separate blinded adjudication precedence from Amendment001. Repeat the original short calibration in a separate subset startup directory before scoring. No new benchmark answers are permitted. Commit complete raw subset/mappings, then blind surface, then judgments, then scores, then effect/evidence diagnostics. Primary categories1–4 and separate category5 use their actual subset denominators; no hardcoded full-corpus counts.

`subset.py` provides a separate stop/preparation, scoring and reporting entry point; the original frozen runner and registration remain unchanged. Its preparation verifies exact request hashes, prompt identities, both-arm completeness and raw EOS status. Before scoring, require committed amendment/code/subset manifests, the original passing frozen-input gate, and all subset raw hashes. The all-source preflight and route tests remain applicable because retrieval has not changed. Test selected-key equality to the intersection of valid response identities; its complement explicitly includes unfinished pairs. Existing missing-gate/no-retry/parser tests remain binding. Full-population claims are intentionally removed rather than lowering a failed outcome bar. There are no benchmark scores at amendment time.

The old runner/server were stopped only after verifying their command lines. Record the stop as user-directed, not an OOM or mechanism failure. Reuse the quiet health/follow-up machinery for scoring and update it to this scope. Stop the scorer's owned server after completion and finish the report/PR.
