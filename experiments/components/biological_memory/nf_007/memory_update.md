# NF-007 Memory Update

NF-007 stops before full registration as `FLOOR_INERT`. NF-006's sealed
own-statement T1 selection at turn 120 already touches all 16 parent-derived
clusters. A floor of one per nonempty cluster forces 0 admissions and displaces
0 incumbents, so treatment would equal control. This is an instrument/design
stop, not evidence that a binding hard floor fails.

Part 1 remains informative. The 119-parent `k=16` partition expands to 791
statements with 6.6471x the candidate load per cluster. Renaissance-art episodes
contribute 194/791 candidates (24.5%), and all 16 clusters are represented in
the sealed T1 selection, yet NF-006 delivers only 1/4 art facts. The residual is
not candidate scarcity or cluster-entry failure; it lies within cluster or at a
finer allocation grain.

Anti-vacuity is now a standing gate lesson: a stopping branch must be shown
capable of not stopping before its result is believed. Part 1 first stopped
vacuously because the evaluator searched for `art` and `monetary` while the
frozen database contains `renaissance_art` and `monetary_policy`. The invalid
artifact is preserved. This follows NF-004's surrogate audit, NF-005's capture
launch check, and NF-006's scored-probe count correction as a fourth
consecutive pre-outcome check that prevented an invalid result or run.

No alternate floor size or cluster count was tried. Any successor must derive
its allocation grain by a separately registered rule, not by sweeping against
Q11.
