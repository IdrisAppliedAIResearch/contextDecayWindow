# DA-016 Phrase-Coded Protected Link Delivery

**Status:** `POST-OUTCOME AVAILABILITY PROTOCOL`
**Date:** August 30, 2026
**Parent:** DA-015 result commit `5a1212a1`
**Standing:** frozen no-model delivery replay on spent LongMemEval
**Planned embedding calls:** 0
**Planned model calls:** 0

## 1. Question

Does DA-015's transferable reversible capacity become exact evidence delivery
when combined with DA-013's frozen temporal pair-then-turn allocator?

## 2. Locked Population and Control

Use the exact 465 DA-013 questions, direct identities, direct order, 7,401 fixed
temporal edges and 16k budget. `DA013_TEMPORAL` is the committed 171-complete
control. Direct identities remain immutable and therefore cannot lose evidence.

## 3. Fixed Treatment

Recompute each direct context with the exact sealed DA-015 codec. Preserve its
phrase declaration order and role-pattern structure.

Traverse edges in DA-013 `TEMPORAL_ORDER`. For a neighbor payload, apply only
the dictionary already declared by the direct context, in declaration order, to
literal occurrences in that payload. Never add or change a phrase from link
text. Charge exact role syntax plus coded member text.

Attempt the complete neighbor pair. On overflow, choose the same DA-010
IDF-weighted source member and attempt its dictionary-coded singleton. Continue
after overflow. Accepted payloads update role state and used characters but not
the phrase dictionary. Ties, edges and member choices remain DA-013 exact.

## 4. Fixed Endpoint and Bar

Before evidence access commit every phrase dictionary, edge order, payload code,
cost and action. Then measure exact complete target-turn delivery.

Report direct, DA-013 temporal and DA-016 complete items; gains/losses, exact
paired test, question-type cells, pair/turn/skip counts, carried identities,
characters, and the fixed one-hop ceiling 250.

Report `PHRASE_LINK_DELIVERY_SIGNAL` only if DA-016 gains at least 10 items over
DA-013 temporal, loses zero, every question-type cell is nonnegative, every gain
has exact admitted-carrier accounting, and direct remains evidence-identical.
Otherwise report `NO_PHRASE_LINK_DELIVERY_SIGNAL`.

## 5. Preflight

- **PF1:** verify DA-015 blind artifact, DA-013 direct/edge/allocation hashes and
  exact 465/7,401 populations.
- **PF2:** test existing-dictionary payload coding, no phrase learning from
  links, exact decode, role overhead, dynamic arrival cost and fallback.
- **PF3:** commit protocol, then blind allocations, before target access.
- **PF4:** require coded payload savings, pair/turn/skip actions and allocation
  differences from DA-013.
- **PF5:** preserve question, direct, phrase, edge, neighbor, member and action
  keys.
- **PF6:** reproduce DA-015 LongMem savings and DA-013 direct identities/order.
- **PF7:** require byte-identical blind and opened replays.
- **PF8:** process all 465 questions and 7,401 edges.
- **PF9:** exact availability is not reader use; both corpora are spent.
- **PF10:** no reader, latency, fresh validation or adoption is authorized.

Stop on hash drift, dictionary change, decode mismatch, direct identity change,
undercharge, unexplained gain/loss or nondeterminism. Do not tune phrase syntax,
edge order, member choice or bar.

