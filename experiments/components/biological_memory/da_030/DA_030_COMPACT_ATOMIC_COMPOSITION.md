# DA-030 Compact Plus Protected Atomic Composition

**Status:** `COMPLETE; PARTIAL_COMPACT_ATOMIC_SIGNAL`
**Date:** August 30, 2026
**Parents:** DA-026 result `adf81349`; DA-028 result `51daaa95`; DA-029 audit `44a475ba`
**Standing:** evidence-blind structural composition on spent NF-004 and LongMemEval
**Planned embedding calls:** 0
**Planned model calls:** 0

## 1. Question

Do two independently protected mechanisms compose: shortest exact codec capacity
followed by atomic completion of linked carriers?

DA-029 leaves 27 wrong-frozen-member residuals (3 NF, 24 LongMem). DA-026 showed
that atomic completion after an immutable pack gains without loss. This study
places that operation after the stronger DA-028 pack without changing either
codec selection or carrier order.

## 2. Locked Controls

Use all 1,098 NF and 465 LongMem questions at 16,000 characters. Reproduce
DA-028 complete delivery 977/208, direct 935/164 and one-hop ceilings 986/250.

Freeze DA-028's selected codec, exact immutable DA-023 identities/order, every
DA-028 added identity/order, final role/history state and exact charge. The
entire DA-028 treatment is the immutable control.

## 3. Atomic Tail

After full DA-028 replay, traverse DA-023's original baseline edge list once in
its original order. For each linked carrier, mark members already present from
direct, baseline, DA-023 additions or DA-028 additions. Attempt each missing
member in frozen lexical member order.

Encode against the row's already selected exact codec and current history.
Admit only if the exact incremental charge fits; continue after overflow.
Never retry a member identity, and never remove, replace, reorder, re-encode or
recharge a DA-028 payload.

No answer, evidence, outcome, question type, conversation, fitted feature,
similarity, threshold or sweep enters member order or admission.

## 4. Blind Gates

Before evidence access commit immutable DA-028 identity/order hashes and
charges, all member attempts/admissions/overflows, final charges and decoded
member identities. Require:

- all 1,563 questions and exact parent hashes;
- byte-identical DA-028 payload sequence and selected codec;
- positive member admissions and overflows in both corpora;
- exact decode, no duplicate member, final charge <=16,000;
- byte-identical replay and zero model, embedding and cache calls.

Stop unopened on any parent mutation, expansion, undercharge, nondeterminism or
incomplete population.

## 5. Outcomes and Disposition

Report exact complete delivery, gains/losses against DA-028, conversation/type
cells, DA-029 blocker rescues, decisive member costs/positions and residual
ceiling gap.

Report `COMPACT_ATOMIC_COMPOSITION_SIGNAL` if NF gains at least two and LongMem
gains at least five, both lose zero and every group is nonnegative. Report
`PARTIAL_COMPACT_ATOMIC_SIGNAL` if either corpus gains at least two with zero
losses and all groups nonnegative. Otherwise report
`NO_COMPACT_ATOMIC_COMPOSITION_SIGNAL`. Any DA-028 loss is a causal stop.

Spent exact availability is not reader use. No runtime, fresh-transfer or
adoption claim follows. Do not tune codec, traversal, member order or budget.
