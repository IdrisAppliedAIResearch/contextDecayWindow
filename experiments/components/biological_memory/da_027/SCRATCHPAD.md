# DA-027 Scratchpad

## 2026-08-30 - Registration

- Replace decimal absolute backreferences with base-36 backward-distance
  pointers; decoded payload identities and order remain identical.
- Re-encode the complete DA-023 pack first. Only its recovered capacity may
  admit frozen-order skipped pairs/members.
- Primary arm excludes DA-026 alternate members to isolate representation.
- Mechanical bar: no expansion and median immutable savings >=64 chars in both
  corpora. Strong delivery bar: >=5/0 in both; partial bar: >=2/0 in either.
- Zero model, embedding and cache calls.

## 2026-08-30 - Blind Stop

- Codec unit gates pass 9/9, including base-36 boundaries, exact replay,
  collision avoidance and malformed/forward reference rejection.
- The 1,563-question allocation stopped before artifact completion and before
  evidence access when a LongMem immutable pack expanded relative to DA-023.
- DA-023 has 449 LongMem backreference renderers and 16 control-renderer rows.
  A compact-only codec cannot dominate the already question-wise renderer
  choice, so DA-027's universal no-expansion gate is false.
- Closed design: forcing compact relative pointers on every question.
- Surviving signal: evidence-blind shortest-exact-codec selection can preserve
  the existing renderer on non-improving rows and use compact pointers only
  where their exact charge is lower. This requires a new registration.

