# DA-028 Scratchpad

## 2026-08-30 - Registration

- DA-027 proved a compact-only codec can expand an existing control-rendered
  LongMem pack; outcomes stayed unopened.
- Compare exact frozen DA-023 and compact-relative charges. Compact wins only
  when strictly shorter; ties and expansion retain DA-023 bytes.
- Only strict compact savings may fund frozen-order skipped carriers. Existing
  payload identities/order and 16k budget are immutable.
- Strong bar >=5/0 in both corpora; partial bar >=2/0 in either, with every
  corresponding group nonnegative.
- Zero model, embedding and cache calls.

## 2026-08-30 - Result

- Blind gate: compact selected on NF 1,098/1,098 and LongMem 461/465; four
  LongMem rows safely retain DA-023. Median savings are 591/556 chars.
- Added actions: NF 2,599; LongMem 548. Every DA-023 payload/order is retained.
- NF delivery 976 -> 977, +1/0. The gain is one prior-consumption residual in
  conv-49. This misses the registered partial >=2 bar for NF.
- LongMem delivery 202 -> 208, +6/0, p=.03125. Five gains are prior-consumption
  residuals and one is wrong-member. Every type is nonnegative.
- Registered disposition is `PARTIAL_SHORTEST_EXACT_CODEC_SIGNAL`: LongMem
  clears >=5/0, while NF does not.
- Capacity is not sufficient by itself. NF admits 2,599 more carriers for one
  completion, so the residual limit has moved from byte supply toward which
  dependency the frozen traversal materializes.

