# DA-034 Scratchpad

## 2026-08-30 - Registration

- NF final gap: 3 wrong-member dependencies, median postpack deficit about 99
  chars. Remove DA-031's one-character opcode from every exact reference.
- Sentinel is shortest all-tilde string absent from every candidate text;
  strict exact fallback to DA-031 prevents expansion.
- Primary isolates representation and excludes atomic completion.
- Strong bar >=2/0; weak exactly 1/0; every conversation nonnegative.
- Zero model, embedding and cache calls.

## 2026-08-30 - Result

- Sentinel selected on 1,098/1,098 NF rows, saving median 823 chars beyond
  DA-031; 371 pairs and 18 turns added, with exact replay and no expansion.
- Delivery remains 983 -> 983: 0 gains, 0 losses. Registered disposition is
  `NO_NF_SENTINEL_VARINT_SIGNAL`.
- Added actions occur late (position p50 15) and follow DA-031's full-pair then
  frozen-member policy. None materializes the three required alternate members.
- Codec capacity alone is closed at this representation. The next supported
  successor must use the recovered sentinel capacity at atomic member
  granularity while retaining the entire DA-031 control.

