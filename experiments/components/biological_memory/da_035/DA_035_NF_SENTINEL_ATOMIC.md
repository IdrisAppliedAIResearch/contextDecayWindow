# DA-035 NF Sentinel Capacity With Atomic Dependencies

**Status:** `COMPLETE; NF_SENTINEL_ATOMIC_SIGNAL`
**Date:** August 30, 2026
**Parent:** DA-034 result commit `3a02591a`
**Standing:** evidence-blind protected NF structural composition
**Planned embedding calls:** 0
**Planned model calls:** 0

## 1. Question

Can sentinel-recovered capacity close NF's final dependency gap when represented
at atomic member granularity rather than full-carrier/frozen-member granularity?

DA-034 recovers median 823 characters beyond DA-031 but gains nothing because
its 389 additions preserve the old materialization unit. DA-032 identifies all
three remaining misses as alternate members costing median 108 characters.

## 2. Locked Control

Use all 1,098 NF questions at 16,000 characters. Freeze DA-031's complete
decoded identity/order and delivery 983. Re-encode only that immutable sequence
with DA-034's fixed sentinel codec. The sentinel charge must be strictly lower;
otherwise retain DA-031 and make no admission.

DA-034's added pairs/turns are not part of the control. This study independently
allocates the same newly recovered sentinel capacity so the materialization unit
is the only treatment difference.

## 3. Atomic Allocation

On sentinel-selected rows, traverse DA-023's original baseline edge list in its
frozen order. Mark all members already present in DA-031. For each carrier,
attempt every missing member independently in lexical member order. Encode
against the current sentinel history, admit on exact fit, and continue after
overflow. Never retry a member.

No answer, evidence, outcome, conversation, similarity, fitted feature,
threshold or sweep enters traversal or admission. No DA-031 payload may be
removed, replaced, reordered, re-encoded under another codec or recharged.

## 4. Blind Gates

Before evidence, commit sentinel charges, immutable order hashes and all member
attempts/admissions/overflows. Require 1,098 exact joins; strict no-expansion;
exact decode; byte-identical DA-031 identity/order; positive member admissions
and overflows; no duplicate member; <=16,000; byte-identical replay; zero calls.
Stop unopened on any mismatch.

## 5. Outcomes and Disposition

Report exact delivery against DA-031, conversations, DA-032 blocker rescues,
member costs/positions and ceiling gap.

Report `NF_SENTINEL_ATOMIC_SIGNAL` for >=2 gains, zero losses and every
conversation nonnegative. Report `WEAK_NF_SENTINEL_ATOMIC_SIGNAL` for exactly
one gain, zero losses and every conversation nonnegative. Otherwise report
`NO_NF_SENTINEL_ATOMIC_SIGNAL`. Any DA-031 loss is a causal stop.

Spent availability only; no reader, runtime, transfer or adoption claim. Do not
tune codec, traversal, member order or budget.
