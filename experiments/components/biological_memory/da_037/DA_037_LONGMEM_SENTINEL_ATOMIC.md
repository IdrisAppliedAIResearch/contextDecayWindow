# DA-037 LongMem Sentinel Capacity With Protected Atomic Tail

**Status:** `COMPLETE; NO_LONGMEM_SENTINEL_ATOMIC_SIGNAL`
**Date:** August 30, 2026
**Parent:** DA-036 result commit `c8d373e7`
**Standing:** evidence-blind protected LongMem structural composition
**Planned embedding calls:** 0
**Planned model calls:** 0

## 1. Question

Can the fixed opcode-free sentinel representation recover enough capacity to
resolve LongMem's dominant initial-size boundary when the recovered capacity is
allocated only to independent atomic members?

DA-036 finds 18/28 residuals too large before DA-033's atomic tail, nine lost
to prior atomic consumption and one multi-carrier conjunction. This tests
representation plus the already supported atomic materialization unit, not a
new relevance or substitution score.

## 2. Locked Control

Use all 465 LongMem questions at 16,000 characters. Freeze DA-031's complete
decoded identities and order, with delivery 211. Re-encode only that immutable
sequence using DA-034's fixed shortest-absent sentinel codec. Retain DA-031 and
make no additions whenever sentinel charge is not strictly lower.

DA-033 additions are not control payloads. They define the frozen evidence-
blind atomic traversal and lexical member order independently replayed inside
sentinel-recovered capacity. No protected DA-031 payload may be removed,
replaced, reordered or displaced.

## 3. Protected Atomic Tail

On sentinel-selected rows, traverse DA-023's original baseline edge list in
its frozen order. Mark every DA-031 member present. Attempt each missing member
independently in lexical member order, admit on exact fit, and continue after
overflow. Never retry a member.

No answer, evidence, outcome, type, similarity, fitted feature, threshold or
sweep enters traversal or admission. Any treatment delivery is a monotone
superset of the protected control identities.

## 4. Blind Gates

Commit all sentinel charges, immutable order hashes and atomic actions before
evidence. Require 465 exact joins; exact sentinel decode; strict no-expansion;
byte-identical DA-031 identity/order; positive admissions and overflows; no
duplicate member; <=16,000; byte-identical replay; zero calls. Stop unopened on
any mismatch.

## 5. Outcomes and Disposition

Report exact delivery against DA-031 and DA-033, question-type cells, DA-036
blocker rescues, member costs/positions and the 250 one-hop ceiling.

Report `LONGMEM_SENTINEL_ATOMIC_SIGNAL` for >=5 gains over DA-033, zero losses
against DA-031, and every type nonnegative against DA-033. Report
`WEAK_LONGMEM_SENTINEL_ATOMIC_SIGNAL` for 2-4 gains over DA-033 under the same
guardrails. Otherwise report `NO_LONGMEM_SENTINEL_ATOMIC_SIGNAL`. Any protected
DA-031 loss is a causal stop.

Spent availability only; no reader, runtime or adoption claim. Do not tune the
codec, traversal, order, member unit or budget.
