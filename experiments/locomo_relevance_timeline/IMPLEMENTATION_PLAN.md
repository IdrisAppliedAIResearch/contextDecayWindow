# LoCoMo relevance timeline implementation

September 7, 2026. User authorizes implementation end to end and explicitly returns orchestration to serial. This supersedes batching in the unregistered LoCoMo draft; earlier locked studies and probes remain unchanged.

Design source: experiments/probes/temporal_da_fusion/LOCOMO_IMPLEMENTATION_DRAFT.md at 87c8ab37. Carry its single arm, full 1,986 occurrences (1,540 primary, 446 separate adversarial), cosine >= .48, complete pair records, chronological order, protected unique anchored relation and before-prefix only. No threshold tuning, retrieval caps, recent block, new resolver, or production adoption.

First implement and execute label-blind Part 1, then commit its findings and a standalone registration before measurement. Freeze prior adapter/parser/prompt code in clean detached checkout 87c8ab37. Preserve exact cached embedding texts; render source dates and dialogue IDs alongside complete pair text. Enumerate omitted source metadata. Stable new keys use conversation content, literal question and duplicate ordinal, never category or generated identifiers. Keep historical keys only for reproduction mapping.

Serial reader and judges: parallel 1, initially 32768 context, native thinking off, no speculative decoding, cache false. Reader seed 5005 and sampling/output 4096 as draft. Tokenize every uncapped input before lock; if necessary resolve a common larger supported context without dropping evidence. Three blind HH001/LV009 semantic judge passes, seeds 9100–9102 and draft sampling. Majority is the scored endpoint. Disagreements are retained and flagged; no extra agent/human adjudication is presumed. Unparseable or truncated judgments stop for explicit instrument repair. Calibrate empty, paraphrase, wrong entity/date and contradiction fixtures with expected outcomes before measurement.

PF1: hash/count source, cached vectors, code and runtime; no cache misses. PF2: execute raw-cosine boundary, empty/full, chronology and anchor applicability. PF3: missing/failed gates and input drift must reject before network; committed gate ancestry enforced. PF4: exercise supported/missing/ambiguous/unsupported routes and both scoring outcomes. PF5: collision-free occurrence keys and reversible source mappings. PF6: reproduce all 128 E prefix identities/payloads and prior LoCoMo pair/vector identities. PF7: stateless selection and cache-free serial reader. PF8: all-population offline check plus fixed two-per-conversation calibration sample included in final answers. PF9: record source-order/event-time, evidence-sufficiency and judge-validity limits. PF10: live answer correctness primary; availability only after committed scores.

Persist pending metadata before each request and raw response immediately after return. Fail closed on transport uncertainty, process exit, empty or capped output; no uncertain automatic retry. A local watchdog records progress, latency, VRAM, server liveness and terminal events. Quiet Codex heartbeat handles failure/stall/completion and subsequent scoring/closeout; no model-driven busy polling during long inference. Use conservative stall alert at max(180 seconds, five times recent median request duration), with hard HTTP timeout 600 seconds. Preserve failures for repair and stop owned servers on terminal exit.

Commit raw completeness before scoring; commit blind votes before evidence analysis. Report absolute/category/conversation correctness and descriptive 10-conversation bootstrap (10,000 draws, seed 93002), plus separate exact abstention. No efficacy bars or historical-control significance. Finish report, error atlas, README, AGENTS digest, handoff and separate draft PR. This plan authorizes instrument implementation and preflight; its executed findings determine the final registration, without changing the requested arm.
