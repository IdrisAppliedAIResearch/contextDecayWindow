# Reader address probe: bounded exploratory plan

User authorization: a quick 5-10 minute reader probe, with an explanation of its answer. This is separate from Study D; no changes to its locked inputs, scores or disposition. No promotion, inferential success bar or model-understanding claim. Request a brief evidence-based justification, not private chain of thought.

Freeze before implementation. Use the Study D reader artifact and launch configuration, one slot, seed 5005, temperature .6, top_p .95, top_k20, min_p0, repeat_penalty1; output cap 1024. Fresh prompt cache disabled. Same empty think suffix. Maximum eight calls, all raw responses persisted immediately. No retry on uncertainty, empty or capped response; stop and report the instrument condition. No extra models or retrieval. Human-readable hand-authored sources use explicit event order. This is direct reader exploration, not a matched Study D experiment.

Part 1: two identical short preflight calls with one address assertion. Require complete nonempty byte-identical answers and an explicit answer plus short cited justification before the six-case exploration. These two observations characterize execution only, with lengths/tokens/stop states retained, not reader competence. No evolving state: every request independent, no response writeback. If this state isolation fails, stop.

Six fixed cases: chronological latest; chronological immediately-before; same before question with records shuffled; immediately-before with two distinct meetings and an intervening update; missing warehouse record explicitly marked as an unavailable update (expected cannot determine); immediately-after the meeting before any new update (warehouse remains active). Last case distinguishes state-at-boundary from next-update lookup. Answers: studio, warehouse, warehouse, studio, cannot determine, warehouse. Short explanations are observations of what the model reports; correct prose can be rationalized and does not certify internal reasoning.

Preflight Part 2, before six cases: PF1 hash/count all six prompts and golds and verify reader/launcher hashes; PF2 two-call observed behavior, exact model props and commands captured; PF3 commit plan before implementation, preflight gate before six cases; PF4 no numerical success/failure mechanism bar, all six expected answers independently traceable, missing case truly underdetermined; PF5 content hashes; PF6 repeat seeded preflight byte equality; PF7 isolated requests/no persistent conversation feedback, demonstrate repeat; PF8 short direct prompts only, no long-context or endurance inference; PF9 easy examples and prompted explanations cannot establish general understanding or explain Study D misses; PF10 score actual final answers descriptively, retain verbatim explanations and do not use fluency as correctness.

Report each question, sources, expected answer and exact model response; describe observed distinctions only. Single seed and tiny chosen population, no transfer/adoption claim. Commit plan, code, prompts, gates, raw responses and report; separate probe PR. Stop the dedicated server after capture. Total execution bounded to ten minutes, no automatic continuation beyond cap.
