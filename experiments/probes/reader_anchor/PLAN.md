# Reader anchor capability diagnostic

User authorizes a small reader probe, September7,2026. Eight constructed short chronological histories test one plausible thread, competing offers, later clarification, retrospective evidence, non-event/proposal, actor identity, missing antecedent, and explicit reference to an older competing thread. This is synthetic capability exploration, not LoCoMo transfer or a retrieval ablation. Current retrieval and paused LoCoMo remain unchanged.

Ask for event resolution, cited event passages, connecting passages, competing candidates, a short evidence-based explanation and whether a source-position cutoff would discard relevant earlier information. These are observable answers, not native thought traces. Native thinkingOFF; pinned Qwen3.8 reader/binary from E restart005, serial,seed5005,sampling .6/.95/topk20/minp0/repeat1/presence0,cachefalse,4096output,40960context. One call/case plus duplicate arithmetic calibration. No tuning/retries or cutoff actually applied.

Freeze histories, questions and expected interpretation before calls; labels kept separate from payload. Descriptive case-by-case inspection after raw completeness commit; no formal aggregate score, human audit or new efficacy bar. Inspect citations mechanically for existence and discuss incorrect/unsupported assertions. A generated explanation is not a causal account of model internals. Clear ambiguous/missing cases should remain unresolved; chronological proximity alone is not sufficient.

Preflight Part1 reuses executed anchor-inactive LoCoMo findings and128 exact E prefix replays; this probes the reader interface next. PF1 hash runtime,code,cases and prompts. PF2 verify exact chronological histories and absence of gold in payload. PF3 missing/failed input gates reject before transport. PF4 explicit resolved/ambiguous/unresolved fixtures reachable. PF5 content-hash case keys. PF6 reproduce native-off template and identical arithmetic response before measurement. PF7 independent uncached requests, no feedback. PF8 eight short histories only, no endurance/generalization claim. PF9 correct-looking citations do not establish inference validity or safe cutoff; unseen competing events remain possible. PF10 live reader responses are observed, availability alone no verdict. Persist raw before inspection; stop owned server on completion/failure. Commit report, handoff and repository digest. No long inference or heartbeat needed for ten short calls.
