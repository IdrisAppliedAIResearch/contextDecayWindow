# Instrument repair

Initial attempt stopped before calibration or stress: default verbosity3 omitted the GPU offload line required by the allocation gate. Preserve initial artifacts. Retry in a new directory with verbosity5; model, capacity policy and workload unchanged. Integer sweep is the implementation's refinement of the planned increasing-slot exploration. This is a telemetry repair, not a capacity failure.
