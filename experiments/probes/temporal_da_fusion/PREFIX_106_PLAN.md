# Review-boundary preservation check on 106 previously correct questions

User authorization: run the previous106 with the same configuration. One fresh seed5005 PREFIX answer for each originally correct before question in full_e_reader/result.json; exclude latest/absent. Same prefix mechanism as plan5085934f, unchanged full source bytes through the existing selector-identified anchor, no refill. Native thinkingOFF, HH001, pinned Qwen3.8/runtime, context32768/output4096,serial/cachefalse and all prior sampling unchanged. No automatic retry, parameter tuning or adoption.

Preflight: hash-bound committed source/result/schedules; require106 unique before identities disjoint from prior22 and union equal all128 before questions. Replay original106 native prompts exactly. Verify all transformed contexts are exact selected prefixes including the unique existing anchor; additionally replay prior22 transformed contexts exactly with this implementation. Negative input gate must prevent requests; parser fixtures and duplicate seeded arithmetic45 calibration before measurement. Commit input gate, then calibration gate, then raw completeness, then blinded scores and any authorized single-agent prose adjudications before aggregate. Labels only check preserved support after context construction. Full140-record sources, stateless transformation, no feedback/endurance claim. Availability is not answer correctness; all106 answers must complete and be scored. Failed gates stop, incomplete captures remain preserved.

Report retained-correct count/106 and regressions with gold/new answer. Combine with committed22/22 prefix recovery only after proving disjoint coverage and identical reader settings: descriptive128-question result assembled from two sequential diagnostic batches, not an independent confirmation or generalization test. Latest/absence were not rerun under this cutoff. No formal efficacy bar; no new historical-baseline attribution. Canonical scorer unchanged; carried user-authorized single-agent prose exceptions disclosed if needed. Stop owned server; update report,README,AGENTS<=400chars,HANDOFF and PR96. Quiet completion/failure hook may finish scoring and closeout.
