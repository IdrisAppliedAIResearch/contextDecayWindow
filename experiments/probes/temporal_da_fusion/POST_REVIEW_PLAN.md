# Remove post-review records on 22 misses

User explicitly authorizes removing post-review records while preserving all preceding evidence and rerunning the22 full E misses. Diagnostic failure-selected sample, not a population score. One treatment call per question, compared descriptively with committed wrong outputs; no fresh control or formal efficacy bar.

For each of the22 score0 before questions in full_e_reader/result.json, use the existing selector-identified anchor turn from the frozen candidate anchors (not gold labels). Retain exactly previously selected episodes at or before that anchor, including the review itself. Remove only selected episodes after it; no refill or new admission. Preserve full source bytes and chronological order. Same HH001 prompt and native-off Qwen reader,seed5005,serial/cachefalse,context32768,output4096 and prior sampling. Capture every raw response without uncertain retries.

Preflight: committed input hashes and22 stable question identities; independently replay all original contexts against scheduled prompt payloads, then verify treatment is their ordered prefix through the existing anchor and every preceding selected source remains byte-identical. Require exactly one identified anchor per question. Gold labels are measurement-only after construction. Verify same native template around original prompts by exact identity,uncropped token fit,negative missing/failed gate preventing requests,negative completeness fixtures and duplicate seeded arithmetic45 calibration. Commit plan before code,input gate before calibration,calibration before inference,raw completeness before blinded scoring,all resolved scores before outcome aggregation. Selection is stateless, existing full140-record sources checked; no new corpus or threshold. Completeness does not certify correct answers. Absorbing-state and endurance tests not applicable to this finite prefix ablation.

Carry canonical scorer and authorized single-agent prose adjudication; final conclusions only,no human/three-pass audit. Report recovery count out of22, all remaining wrong answers and all new responses,removed/retained counts,actual output use/runtime. Failure selection and no fresh repeated control limit attribution. This is diagnostic truncation at an identified temporal boundary,not a general retrieval-budget policy or production change. Update report,handoff,README/AGENTS and PR96. Stop owned server and preserve prior artifacts.
