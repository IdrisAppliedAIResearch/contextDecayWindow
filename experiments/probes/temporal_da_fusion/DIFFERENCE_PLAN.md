# Difference probe: linked admissions versus displaced evidence

Exploratory follow-up authorized September 6: probe the six gains/seven losses and take the analysis through to a recommendation. Prior result and discordant identities are already exposed. This is not validation on unseen data. No model/embedding calls, no adoption, and no new success bar.

## Scope fixed before new measurement

1. Reproduce the committed fusion selected identities and exact context digests on all 192 histories. Gate any new measurement on this replay.
2. Extract available-at-retrieval features for every added and removed source on all 128 answerable before histories, not only the 13 discordances: direct CC80 rank/score, raw cosine, signed source distance to anchor, before-anchor status, linked parent rank, link direction, parent selected status. Compare required versus non-required records only in the measurement stage. Report full rows and distributions. Source first sentences are available for human explanation; no gold-derived state parser is built.
3. Run three fixed structural ablations: forward links only; backward links only; links only when the child is strictly before the existing anchor. Original seed stream, protected temporal block, semantic fallback, packing, rendering and continuity stay fixed. These are diagnostics motivated by exposed forward rescues and known temporal scope, not independently selected or validated policies. Do not tune cutoffs, reserve shares, weights or number of seeds.
4. Test each ablation on all 192 histories and separately report the 115 before questions outside the original discordances. Those 115 were already exposed in the first probe, so they are a remainder check, not a holdout. Report complete evidence gains/losses, retained original rescues, original losses repaired, new harms, absence context changes, record counts and changes in source identities.
5. For the seven original losses, assess displacement mechanically: restore each missing carrier at its original direct-stream position while retaining other links (suppress all early linked emissions of that carrier is not a restoration and must not be mislabeled). Instead report its direct rank, fusion order position and selected boundary; no invented one-to-one exchange attribution. Multiple admissions share a budget, so do not name one neighbor as the unique cause without a separate causal intervention.

## Preflight

PF1 hash committed original outputs/gate, curves, sources, labels and new code. PF2 forward/backward/scope fixtures prove the named link filters; compare unrestricted adapter order with original DA traversal on all histories. PF3 require passing exact-replay gate before outcomes; failed-gate fixture. PF4 no efficacy bars; positive gain/loss/empty-label measurement fixtures. PF5 stable source/query identities and content digests from original outputs. PF6 exact original fusion identities/context hashes, plus unrestricted-order equality. PF7 frozen finite nonrecursive stream; unique full permutation for every filter on real histories and two-session fixture. PF8 all140-record histories; no online feedback/transfer test. PF9 features can separate obvious non-evidence yet fail to select the decisive update; remainder results are not fresh generalization. Availability is not reader correctness. PF10 reader required for success; this probe yields a recommendation and preserves all prior results.

Commit plan alone, implement and commit, run label-free replay/ablations, commit passing gate/outputs, then analyze. No automatic live run. Any recommendation names whether it follows from mechanism, exploratory measurement, or untested inference.
