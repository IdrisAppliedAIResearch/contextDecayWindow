# Initial relevance timeline reader check

Exploratory continuation of RELEVANCE_PLAN.md (1676445b), authorized by the user's relevance-timeline request. Offline candidate/gate locked d275e9ce. Compare frozen C1_CHRONO_NO_RECENT against ANCHORED raw-cosine>=.48 uncapped timeline, both chronological/no additive recency. PURE remains diagnostic only. Same16 question ids as chronology probe;fresh32 calls,seed5005,alternating per-question arm order. Exposed sample, no efficacy bars or adoption.

Use the same pinned reader/native thinking-off template/serial sampling/cachefalse/32768 context. Common output cap4096 for both arms, fixed before tokenization and calls; this is lower than the prior16384 reserve. Native-off confirmation completed with max2155 output tokens and the chronology probe returned short canonical finals;4096 gives room without using a16k generation reservation to block an approximately18k input. Never truncate inputs. Any prompt+4096 exceeding32768 stops before measurement. A capped/incomplete response stops, without raising the cap or retrying. Record this runtime difference and compare fresh arms only.

Reuse verified chronology request/capture/parser code, hash both wrapper and helper. Free dedicated8097 server only; hidden process, pinned executable/model/libraries, recordPID/command/properties, verify and stop ownedserver after completion/failure. Two native-off arithmetic calls must return identical45 with no thought block. Calibration and input gates committed before32calls; raw complete responses committed before blind canonical scoring; scores before aggregate. Per-request pending journal and immediate fsync; no uncertain retry. Parent membership/chronology gates and .48 boundary/anchor fixtures carry by hash. Fail-gate fault injection must run before calibration, proven zero network calls.

PF1 input/runtime hashes,32 schedule calls; PF2 native-off template and emitted final, exact baseline16prompt reproduction; PF3 early failed-gate fixture and committedpositivegates; PF4 correct/wrong/empty/ambiguousfinalfixtures,noefficacybars; PF5 prompt/source/contentids; PF6 baseline native identity and duplicate calibration; PF7 nofeedback; PF8 fulluncropped timelines,16casesone seed; PF9 complete evidence may stillyieldwronganswers andall-relevant historyisnotcompletion; PF10 final-answer correctness for12before and4guards reportedseparately, noavailability-onlyverdict. Prioruser single-agent scoringauthorization carries if needed; nohuman-auditclaim.
