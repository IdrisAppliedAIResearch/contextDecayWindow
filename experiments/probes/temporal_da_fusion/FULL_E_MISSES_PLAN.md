# Full E evidence-use miss audit

User requests diagnosing missed immediately-before questions. Scope: all22 incorrect answers in the committed full single-arm run; no inference, changed retrieval, scoring, or parameters. Correct the premise if evidence is present rather than assuming an upstream miss.

Preflight: hash-check committed result/scores/schedule/raw completeness and candidate gate; join22 by content ids to192 sources. Replay all22 rendered selected contexts into their exact scheduled prompts; require all gold source ids, literal sufficient-set statements, and anchor/target records actually appear. Independently reconstruct the latest explicit effective location before each anchor from source text and compare to reference; retain every intervening source statement to catch label errors. This is a static full140-record audit, no feedback/ablation or new efficacy bar. Demonstrate wrong/truncated/missing support fixtures fail the evidence check. Commit audit implementation before execution.

For each wrong answer, report target and anchor turns, raw target cosine, closest earlier effective update and first later update, proposal/future qualifier, and every effective source turn matching the returned location. Compare wrong value with those alternatives using nonexclusive matches; repeated values cannot identify which record caused an answer. Record intervening filler mentions of the returned location as descriptive only. No thinking trace or causal mechanism claim from output coincidence. Existing final scores remain unchanged. Report all22 cases and aggregate patterns, update handoff/README/AGENTS and PR96; no further reader calls authorized by this audit.
