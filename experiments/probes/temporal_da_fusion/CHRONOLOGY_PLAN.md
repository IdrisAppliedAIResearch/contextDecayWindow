# Recency removal and chronological presentation probe

User authorization September 6: remove the32 recency bars and probe chronological presentation of the same retrieval. Interpret as removing the additive last32 exchanges from reader context, not changing the retrieved selection or rerunning retrieval. C1 remains the current baseline. This is an exploratory renderer/reader probe, not a deployed change or new study verdict.

## Frozen comparison

Three arms: ORIGINAL exact C1 context; NO_RECENT same selected retrieval in its current order with additive recent context removed; CHRONO_NO_RECENT exactly those same retrieved records sorted by source turn ascending, with recent context removed. Use the existing renderer's empty `<recent_context/>` marker; no32 source records remain in that channel. Full episode contents, turn ids, question, instruction and retrieval membership are identical across arms; no refill from newly freed space. This distinguishes recency removal from subsequent chronological ordering, though no claim about chronology with recency present is possible.

Offline verify all192 histories and write all transformed contexts. Reader sample: first3 question content ids per straight/irrelevant/future/proposal stratum, plus first2 each latest/absent:16questions,48calls. Selection is deterministic and does not depend on correctness or evidence completeness. This is exposed synthetic data, not heldout validation. Seed5005 for every paired arm. Per-question arm order rotates through the3arms by sample position to distribute runtime order. No replicate/general accuracy/significance claim.

Runtime follows Study E restart005: pinned local model/server/libraries; native thinking off, one slot, context32768, cap16384, cachefalse, same sampling settings, no speculative decoding. Verify off template against saved baseline native prompt, flags/props and two identical short arithmetic calibration calls before the sample. No hidden/explicit reasoning prompt added. Use dedicated free localhost8097, hidden server. Record hashes, properties,PID, launch. Stop only the verified owned PID on completion/failure. No retries after uncertain request, no cap escalation. Capture raw output immediately with write-ahead journal and fsync. Non-EOS, truncation, empty final or unexpected native thought block stops the run.

## Preflight and commit order

Plan-only commit before code. Commit renderer/code and16question inputs before calibration. Commit input gate before calls; commit duplicate calibration gate before48measurement calls. Commit raw complete capture before scores, then interpretation. All source files remain unchanged.

PF1: hash/count original sources/traces/prompts/runtime pins and prior prompt store. PF2: all192 exact ORIGINAL payloads, selected source fragment multiset equal in all3arms, NO_RECENT source order unchanged and CHRONO strictly ascending; no recent source ids in removed channels. Native-off template/response verification. PF3: missing/failed gate prevents measurement; planted negative fixtures. PF4: no success bars; scorer fixtures correct/wrong/empty/contradictory final. PF5: content ids,prompt+seed hashes. PF6: all192 exact original context hashes,16 native ORIGINAL prompts match prior saved template; duplicate calibration content identical. PF7: no feedback/cache/writeback. PF8: full-length prompts tokenized and context+output allowance checked;16case probe cannot establish general transfer. PF9: identical evidence does not imply identical answer; absence/latest guards and incomplete-evidence cases reported separately. PF10: score actual final answers, not source presence. User's prior single-agent scoring authorization carries; unambiguous canonical location/abstention can score mechanically, ambiguous finals require recorded agent rationale. No human/independent-audit claim.

Report paired changes ORIGINAL→NO_RECENT and NO_RECENT→CHRONO_NO_RECENT, primary12 and guards4 separately, per-question answers, word/character/token changes and runtime. Do not silently substitute earlier baseline answers for fresh paired calls. If a run gate fails, preserve it as instrument failure and do not report uncompleted cases as wrong answers.
