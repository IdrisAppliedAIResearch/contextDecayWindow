# Reader batch capacity probe

Engineering diagnostic authorized by the user's explicit request to probe GPU spare VRAM and maximum batching. This overrides AGENTS serial default only for this probe; completed studies remain untouched. No LoCoMo answers or retrieval changes.

Use the pinned E reader binary/model, native thinking off, q8_0 KV, GPU-only layers, no speculative decoding, fixed seed5005 and cache disabled. Preserve 32768 context tokens per slot by allocating total context=slots*32768. Distinguish parallel request slots from token batch/ubatch settings. Inspect actual server properties and allocation logs.

Explore startup allocation for increasing slots (1,2,4,8, then integer refinement) until free dedicated VRAM is below 1536 MiB, allocation fails, or GPU offload is incomplete; stop that server promptly. Do not deliberately stress an already below-headroom allocation. Memory linear extrapolation may skip clearly unsafe sizes. Maximum claim is conditional on this context/cache/model and current desktop use.

At the highest qualifying allocation, verify native template and repeated short seeded arithmetic response, then send simultaneous synthetic requests with distinct prefixes and approximately 28k input tokens, allowing 4096 output tokens per slot. Use bounded 64-token forced decoding for capacity/throughput stress (ignore EOS), explicitly not scored answers. Repeat highest passing batch twice; verify all slots actually overlap, full prompts consumed, no truncation/context shift, and minimum free VRAM >=1536 MiB. If failure, step down. Record maximum allocation versus highest stress-validated concurrency separately. No guarantee of sustained 4096-token decoding or full LoCoMo input fit; registration still needs full-population tokenization. Persist outputs/errors/telemetry and stop only owned servers.

Preflight: PF1 stream-hash pinned binary/model and launch source; PF2 empirical startup slot/context/all-GPU properties and telemetry; PF3 committed plan/code and input gate required before stress, negative missing-gate fixture; PF4 memory-headroom positive/negative fixtures, no efficacy bars; PF5 SHA256 synthetic prompt identities; PF6 reproduce pinned launch options except explicit parallel/context/port overrides; PF7 independent requests cachefalse, no retrieval feedback, verify distinct slots; PF8 two simultaneous waves at near-window input length, not long-duration certification; PF9 allocation alone is not active capacity, short decoding does not prove maximum-output endurance, WDDM telemetry is sampled; PF10 engineering throughput only, no answer correctness claim. Commit raw results before analysis and update draft/handoff with qualified recommendation.
