# Four incomplete-evidence questions: source-to-prompt audit

User authorizes diagnosing missed retrieval, retaining fully deterministic memory and existing reader settings. Audit all four broad20 incomplete annotation cases, including the correctly answered patriotic question. Six? Count exact missing dialogue IDs from frozen results rather than assume. No new reader/embedding calls, threshold tuning, retrieval changes or efficacy test.

Trace each annotated passage through original source (including separately stored image captions), exact cached pair text, normalized cosine, rank, .48 selection, chronological renderer and delivered prompt. Recompute scores from immutable caches, replay all30 selected IDs and prompt digests before interpreting the four misses. Record nearest selected source neighbors and their content, threshold margin, source/adapter omissions, and whether gold is a literal fact or an inferred recommendation. Annotation loss is not automatically answer impossibility. Distinguish known mechanical exclusion from hypotheses about low semantic score.

PF1 hash source,caches,manifests,prior inputs/results. PF2 exact raw cosine and >=.48 selection replay. PF3 replay assertions before audit output. PF4 include selected controls and excluded annotated carriers; missing IDs must resolve uniquely. PF5 content IDs and reversible dialogue mappings. PF6 all30 current selections/payloads exact and cached scores reproduced numerically. PF7 no state/feedback. PF8 four selected diagnostic cases only. PF9 annotation completeness not sufficiency, image-only facts not recovered by threshold, associative recommendations not verbatim source. PF10 prior reader scores remain unchanged; this audit is availability diagnosis only. Preserve report and raw trace before recommendations, update handoff and PR97.
