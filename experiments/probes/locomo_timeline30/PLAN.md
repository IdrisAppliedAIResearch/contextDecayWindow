# Thirty-question LoCoMo current-arm reader probe

User authorizes a small LoCoMo performance probe. Deterministic memory architecture is binding: no model output may affect retrieval, linking, ordering or context selection. Each question receives one answering call. Separate judge calls are evaluation only and never feed memory or reader inputs.

Freeze30 distinct primary(category1–4) occurrences from the existing1,986 chronological relevance prompts: broad20 = lowest two content keys per conversation among primary questions; temporal10 = lowest remaining category2 key per conversation. Keep group labels separate; do not combine into an unbiased population score. No gold/evidence/historical outcomes in sampling. Natural corpus has been used extensively; descriptive development probe, not independent confirmation. No inference beyond30 authorized here; full LoCoMo remains paused.

Carry exact existing native prompts and selections from experiments/locomo_relevance_timeline/artifacts: raw cosine>=.48,full pairs,chronological,no cap/no recency,unchanged unsupported anchor parser(no cutoff activates). HH001 brief answer-only prompt, no anchor or explanation instructions. Same pinned Qwen3.8 reader,E restart005 binary/model,nativeOFF,serial,seed5005,temperature.6,top_p.95,top_k20,min_p0,repeat1,presence0,cachefalse,40960context,4096output. No native thoughts. Two identical arithmetic calibrations before answers. Persist pending and raw per call, fail closed on empty/capped/uncertain calls, no silent retry.

After committed complete raw answers, create blind question/gold/answer scoring surface. Three local HH001 semantic judge passes,seeds9100–9102,temp.2,top_p.9,other settings unchanged; evaluation calls only. Calibrate empty/paraphrase/wrong entity/date/contradiction fixtures before judging. Persist raw90 votes before parsing/aggregation. Majority endpoint; flag every disagreement and inspect cases with single-agent qualitative review, disclosed rather than independent human audit. Primary report remains majority and any review concern is explicit; no automatic revised scores. Report broad20 and supplemental temporal10 separately, every question/gold/answer, output/runtime, and post-score annotated evidence availability. No significance/WORKS bar/generalization/chronology-causality claim without a comparator.

Preflight Part1 existing exact1,986 native fits, raw full-context comparison c3555066,128Eprefix replay supply empirical starting point. PF1 verify hashes,population/pins. PF2 selected native prompts exact, zero anchor activations. PF3 missing/failed gate and calibration reject before measurement; commit gates first. PF4 all source groups populated and scoring positive/negative controls. PF5 content-key/occurrence identities, disjoint20+10. PF6 exact existing native prompt replay and seeded arithmetic repeat. PF7 stateless uncached serial calls, no model-dependent memory. PF8 small descriptive coverage only. PF9 annotations incomplete; judge agreement not proof; sample not accuracy oracle. PF10 live answer correctness endpoint; availability only diagnostic after votes committed.

Close with report,raw/score artifacts,README/AGENTS/handoff and existing draftPR97 update. Stop all owned servers; this short probe needs no long-run hook. No architecture change or full-study resume.
