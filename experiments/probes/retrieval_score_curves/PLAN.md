# Existing-run score-curve probe

2026-09-06. User authorization: inspect existing runs for clues about a natural score-decay threshold; curves are not an oracle. Exploratory descriptive replay only. No new model/embedding calls, retrieval policy, resource-limit study, cutoff fitting, success bar or changed prior result. The design-only commit containing this file precedes implementation.

Use all192 Study E confirmation questions, highlighting its128 before questions and the19 C1 incomplete-evidence cases without restricting the analysis to misses. Use Study D's existing committed rank-score arrays as a separate historical contrast, preserving all question types and denominators. Do not pool studies. E scores are reconstructed only from frozen persisted vectors and the pinned control ranker; require every complete ranked identity sequence to match its sealed trace before analysis. D rank arrays are already preserved and must agree with their query/source identities and score ordering. No regenerated vectors.

Part1: save raw cosine, BM25 and CC80 score curves in descending correlation order, plus CC80 scores along E's chronological temporal order. State that CC80 is per-query min-max-normalized80:20 cosine/BM25, not a calibrated probability; temporal order is not correlation order. Separate full-store curves, non-continuity candidates, and before-eligible candidates so a corpus/continuity seam cannot masquerade as answer completion.

Describe adjacent score drops, largest-drop locations (earliest tie), normalized required-record positions and required score/top-score ratios. For each curve, use the single largest adjacent drop as a fixed illustrative arithmetic boundary; retain the prefix ending before it. This is a diagnostic counterfactual, not a selected policy. Save the prefix size, fraction retained, required-source completeness, and separately survival of state-setting target vs anchor/qualifier for E. Report distributions and compare existing complete/incomplete strata; no threshold chosen to maximize outcomes. Flat or single-item curves have no identifiable gap and must be marked undefined rather than passing. Report any gap's relation to record type and candidate-pool composition.

Preflight: PF1 hash/count sources,labels,vectors,rank arrays,traces and pinned ranker; no unread holdout. PF2 empirical exact replay and distributions in saved curves, checking temporal nonmonotonicity. PF3 replay gate and label-free curve extraction committed before diagnostic annotation. PF4 positive separated and flat/no-gap fixtures, monotone/nonmonotone fixtures; no efficacy bar. PF5 source-content identities and score hashes. PF6 every E rank order matches sealed trace; D source/score/order identity checks. PF7 no feedback or writeback. PF8 full existing populations, no endurance/general-transfer claim. PF9 rank decay is guaranteed by sorting, the largest gap can reflect corpus templates or normalization, and records below it may remain essential. Required-source annotations are retrospective diagnostics, never inputs to gap choice. PF10 no reader claims: existing reader scores remain unchanged, no new reader run.

Use independent CPU workers for offline reconstruction if useful; constrain each worker's numerical libraries to one thread to preserve vector arithmetic, record workers and elapsed time. Render a standalone scientific figure with curves/dispersion and illustrative annotated examples selected by lowest stable question identity within existing complete/miss strata. Inspect the plot visually. Preserve code,raw curves,replay gate,diagnostic rows,summary and report. Update memory and PR95 with a clearly exploratory addendum, not adoption. Do not infer a universal threshold or tune a rule from these outcomes.
