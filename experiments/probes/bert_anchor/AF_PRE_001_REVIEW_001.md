# AF-PRE-001 — external critique and adopted revisions

**Date:** September 20, 2026. **Critique target:** plan v1 at `79eb8c1d`. **Disposition:** REWRITE; v2 supersedes v1 before any gate, label, or run. Reviewer: adversarial senior-scientist subagent reading plan, `AGENTS.md`, `PREFLIGHT.md`, `corpus.py`, `_timeline.py`, the sealed relevance/chronology/reader-anchor reports, and the DA post-mortem.

## Findings and resolutions

| # | Finding (verified) | Resolution in v2 |
|---|---|---|
| 1 | Partition inverted and contested: `nf004_measurement.py:23-24` sets dev=conv-41/42/47/48, sealed holdout=26/30/43/44/49/50; the AV-arc handoff inverts the names (six=dev 848, four=confirmation 692). v1's literal sample frame points at a sealed-confirmation set under one regime. | User ruling: **no partition authority**. Dev pool is a seeded random stratified sample across all conversations; every LoCoMo number is characterization; no surface is confirmation-grade. |
| 2 | E is lexically solved by construction (`corpus.py:22` enforces a unique meeting-name carrier; the event sentence is the exchange's first line). `LEX`=gold=128/128; the CE-vs-LEX contrast on original E queries is structurally empty. | Added registered **name-stripped paraphrase** E surface (deterministic template rewrite, gold anchors unchanged); paraphrase E is the disposition surface; original E is fidelity check only. |
| 3 | "Zero losses" guaranteed by monotone union selection (`_timeline.py:56-58`); availability and 17-anchor recovery are one number twice — the exact surrogate the DA post-mortem §8.4 orders distrusted. | Zero-loss clause deleted; structural floor stated; one registered quantity (anchor recovery with interval), availability shown only as derived identity. |
| 4 | Dev gold single-annotator with LEX inside the review loop, then LEX is the bar to beat — circular. | Two independent blind passes on the full sample, arm-blind and LEX-blind; Cohen's κ registered; κ<.7 ⇒ instrument failure per §9.2, no dev disposition. Tie/multi-evidence rule stated. |
| 5 | No reader obligation — repeats the DA arc's registered failure (PF10 printed, never tripped). | WORKS binding-successor: registered paired native-reader pilot (anchor-recovered items + matched controls, ~40 items x 2 contexts), bar and cost stated now, before any fine-tuning study. Non-trip condition: WORKS reading never says "adequate" without it. |
| 6 | Bars uncalibrated ("above chance" vacuous at ~1/500; "strictly above" = net +1; 9/17 within one SE of noise; PF3/PF4 conflict from post-seal dev labels). | 60-item calibration slice labeled and committed **before** predictions, for PF4 reachability only, excluded from scoring; paired exact McNemar with registered net margin; intervals registered per §9.3. |
| 7 | 512-token/max-pool prior miscited (2.5k chars is LongMem episodes; E exchanges fit one window). | Pooling language stays conditional on Part 1 measured token/window distributions; if >90% single-window the pooling clause is deleted before lock. |
| 8 | NLI relation mismatch + template freedom; two new model families strain §4 Scope; missing free arms. | NLI dropped from disposition, deferred as post-hoc diagnostic only. Added BM25 lexical floor and the zero-cost cached Qwen3 bi-encoder arm. Authorization for the CE model download recorded here. |
| 9 | Instrument identity fixtures missing; tie and horizon-violation policies undefined. | Planted positive + adversarial hard-negative fixtures per arm with committed expected outcomes; deterministic tie-break by source turn; predicted anchor past horizon or absent ⇒ fall back to `PURE` for that question, counted as miss. |
| 10-13 | Double-reported E quantities; noisy LoCoMo evidence annotations; point bars without intervals; the 17 uncharacterized (all proposal/irrelevant/future per sealed artifact). | Single reported quantity; annotation noise folded into the κ design; intervals registered; the 17's composition is Part 1 deliverable (a), with an explicit RNG-artifact caveat if composition holds. |

Retained from v1 unchanged: sealing order, PF6 replay gate, prediction-before-labels commit order, mechanism/measurement code boundary, offline zero-reader scope of the probe itself.
