# Native thinking-on template characterization

2026-09-06, before any calibration or selected-case completion. Plan c6b955d3's anticipated boundary-only difference failed during preparation, so no answer calls were made. The native thinking-on template inserts this system message as well as opening the thinking segment:

> Reasoning effort is set to xhigh. Please think carefully through the task, validate key assumptions, consider plausible alternatives, and prioritize correctness, consistency, and clarity in the final answer.

The requested high effort and omitted effort both produced this exact prefix with this installed server/model. The off template reproduced the old prompt exactly. This is the native behavior of the configuration requested by the user, not an added experimenter-written explanation instruction. The original user payload and evidence remain unchanged.

The probe therefore compares native thinking-on configuration (this prefix plus open thinking boundary) against the prior native off configuration. It does not isolate a boundary-only or internal-computation-only effect. Replace the boundary-only assertion with exact validation of this observed prefix and the open boundary. Preserve the stopped preparation directory `artifacts`; use `artifacts_v2` for the fresh preparation and run. No selected population, seed, cap, scoring rule or sample size changes. This design note precedes the corresponding implementation correction; user authorization to inspect native thinking remains in force.
