# Study E thinking-on error probe

2026-09-06. User-authorized exploratory follow-up: turn native thinking on for complete-evidence C1 failures and retain the emitted thinking to examine where responses go wrong. This is separate from the completed Study E registration and does not revise its scores. The commit containing this plan alone is the design anchor; implementation follows it.

## Scope and selection

The frozen restart005 result has 243 complete-evidence wrong C1 logical answers across 97 distinct questions (straight27, irrelevant26, future19, proposal25). Start small: three questions per condition, twelve total. Within each condition sort question content identities lexicographically and take the first three; for each use the lowest originally failed seed. Selection uses no new output or handpicked error narrative. Preserve the full 243-row eligibility inventory and selected identities before new calls. A twelve-case recovery count cannot be extrapolated to all243, or to general thinking-on accuracy. Prior correctness-selected sampling also makes this unsuitable as an unbiased treatment-effect estimate.

Keep each selected source-established reference, complete delivered evidence and carried instruction/query payload exactly unchanged. Use the same model, executable, library pins, serial slot, cache=false,32768 context and16384 total output cap as restart005. Enable native thinking with `--reasoning on --reasoning-budget -1` and `enable_thinking=true`, reasoning_effort=high for template rendering. Capture raw completion with reasoning_format=none, which preserves emitted thought text rather than discarding it. Assert native off rendering reproduces the prior full prompt byte-for-byte; assert on differs only in the assistant thinking boundary, with user payload identical. No explanatory instruction or answer grammar is added.

Use a separate dedicated localhost8097 server only when the port is free. Record command, PID, properties and runtime hashes. No retrieval, embedding, source generation or response writeback occurs. Preserve the study files unchanged.

## Preflight and sequence

Part1 runtime exploration: render on/off, tokenize all twelve prompts, and run a short arithmetic calibration twice at seed5005 with thinking on. Preserve both raw responses. Require byte-identical complete responses with a closing thinking boundary and an explicit final answer. This checks native thought capture and seeded reproducibility, not task correctness or faithful introspection. A failed calibration stops before the selected cases.

Commit design before code; commit code and frozen inputs before calibration; commit calibration and input gate before selected calls. PF1: verify all input/runtime hashes,243 eligible rows,97 questions,12 selected with complete exact required source fragments. PF2: empirical native-template difference and raw thinking/final capture. PF3: missing input/calibration lock rejects before calls; persisted uncertain journal precedes each request. PF4: parser fixtures cover valid final, missing closure, blank final and cap; no efficacy bar. PF5: full prompt+seed hashes and original content IDs. PF6: exact reproduction of prior off-template bytes and duplicate calibration. PF7: no feedback, cache=false, input/code hashes before/after. PF8: all twelve full-length prompts fit context+cap; short calibration cannot guarantee long-case completion. PF9: thought text is a model-produced explanation, not guaranteed access to causal internal processing; exact evidence presence does not certify interpretation. PF10: judge explicit final correctness, retain reasoning as diagnostic only.

Each request is serial, journaled before submission and fsynced immediately after response. No retries on uncertain calls. Timeout600s. A timeout, capped/non-EOS/truncated output, missing thinking closure or missing final stops new calls; preserve completed and uncertain records and report the instrument limitation separately from wrong final answers. No cap escalation. Stop only the verified dedicated server on terminal state.

## Scoring and interpretation

Commit completeness/raw capture before scoring. Score final content after the native closing `</think>` boundary, using unchanged Study E reference and semantic rules; reasoning alone cannot earn credit. User's existing single-agent scoring authorization carries to this exploratory probe. Calibrate against Amendment003's no-answer/contradiction examples. Preserve per-item reference, final answer, score and exact final evidence before detailed thinking analysis. No human/independent-audit claim.

Then inspect each emitted reasoning trace against actual delivered records. For each residual error record the first visible unsupported or mistaken step with exact excerpt and source counterevidence; distinguish observations from inferred causes and mark unclear cases unresolved. Do not equate attention to a record with correct use, or fluent explanation with faithful reasoning. Summarize corrected and still-wrong final counts out of completed selected cases, disclose incomplete attempts, and provide user-legible examples for discussion. No significance test, success disposition, general accuracy estimate or new retrieval policy. A further run over all97 or243 needs the user's scope choice.

Deliver plan, selection/gates, raw thinking and finals, scores, case analysis and report under this probe directory. Update memory and PR95 with a clearly exploratory addendum; original Study E verdict unchanged. No merge or adoption.
