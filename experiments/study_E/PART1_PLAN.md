# Study E Part 1 execution plan

Authorized by user end-to-end implementation on 2026-09-06. This supersedes the draft's authorization-status prose, not any locked study. Commit this plan before development implementation. Confirmation seeds 94001-94032 remain unopened until final registration. No adoption.

Stage A: replay all 224 Study D C1 contexts from isolated Study D checkout at 05ef90e2 and original engine checkout 5ebda1ef. Use sealed sources/vectors, zero new calls. Implement the sole before-only reverse-turn ordering in a new module, preserving original eligible sets, anchor and packer. Repeat all calls and characterize all 32 old T1s and named degenerate fixtures. This is exposed development, not confirmation.

Stage B: develop exactly four 140-episode sessions at seeds 93001-93004 with four before conditions, latest and active absent-field guards from the draft. Initial concrete generator: six named subjects, anchor positions sampled without replacement from 45-95; prior true update 3-12 turns earlier, nearer distractor at anchor-1 or -2 where required; unique allocation slots sampled/resolved before text generation. Each subject gets at least three explicit prior updates; unrelated operational prose makes whole records about 700-900 chars. Remaining early turns carry subject mentions/revisions with explicit ledger semantics; after turn108 use unrelated continuity. Six subject histories share one session. Values sampled from office/warehouse/studio/depot/workshop/laboratory/annex/hangar. Lock exact generator only after development audit. Detect saturation or inactive harms before confirmation; any corpus repair is documented as development, never silently tuned on confirmation.

Stage C: unchanged Study D reader and prompt, five seeds as draft, but development captures at seed5005 only. Maximum 35 development calls: 24 C0/C1 before cases selected by deterministic prefix schedule plus latest/absence calibration and maximum-prompt duplicates, final exact schedule committed before generation. No correctness-based extra attempts. Prefix repeats must match. Tokenizer and runtime checks precede reader. All raw outputs persist/fsync, uncertain request stops. New embedding texts one at a time in eight independent one-thread CPU workers, vectors persisted; worker sentinels identical. No confirmation inference in Part1.

Scoring design: mechanical bare vocabulary values and canonical abstention only, exact normalization; noncanonical responses use blinded human review. Preserve the Study D prompt. The user may review a legible packet if needed; no single-agent waiver presumed. Before lock, define rubric for self-corrections: final explicitly adopted answer judged, retracted earlier value not itself disqualifying, unresolved competing answers incorrect; explanation cannot supply credit without an adopted answer. All unresolved surfaces stay pending, and all scored artifacts committed before unsealing. Development observations do not relax this rule.

Preflight: PF1 hashes/counts all source/cache/model inputs; PF2 real traces for route behavior; PF3 plan before code, stage gates before reader, final registration before confirmation; PF4 preserve positive and negative fixtures and demonstrate candidate/guard populations; PF5 stable source/prompt/reference keys; PF6 224/224 prior prompt identity; PF7 repeat pure calls plus source hashes unchanged, no feedback; PF8 140-source development snapshots not endurance; PF9 saturation, qualifier carriers, temporal signal vs nearest mention, availability not reader correctness; PF10 reader is final success endpoint, Part1 carries no disposition. Every item requires artifact evidence before corresponding stage. Stop on broken identity or unresolved instrument ambiguity.
