# Amendment 004 implementation note

The stopped8192-token response took114.69seconds to decode. Doubling the allowance can approach the old240-second HTTP timeout before prompt evaluation is included. The continuation therefore permits600seconds for a single HTTP request, with no retries. This accommodates the approved output allowance; it does not change the model, sampler or success criteria. Any timeout still stops with its pending journal preserved.

Execution is split into `prefix` and `resume`: two calibration calls plus the single repair first; commit its successful identity gates before issuing the remaining3052calls. On completion preserve all new captures and an indexed combined archive. Combined selected capture has3508records; actual requests across both attempts total3511, including the original capped call and two additional calibrations. Score all3840original logical answers exactly once.
