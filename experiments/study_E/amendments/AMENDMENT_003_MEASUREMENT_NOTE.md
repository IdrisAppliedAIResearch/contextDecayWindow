# Amendment 003 measurement implementation correction

Recorded during embedding preparation, before inspecting readiness results or making reader calls. The inherited availability helper searches for required sentences anywhere in the prompt. Competing histories intentionally reuse update wording and may repeat the same value at an older turn. Therefore text presence alone can pass without delivering the designated temporal carrier. This is the surrogate failure explicitly excluded by Amendment 003.

Preserve the initial preparation outputs but treat their text-only availability counts and readiness gate as invalid for reader authorization. Recompute exact availability from the required source identities and the actual final packed identities, additionally checking reference sentences in the rendered prompt. All required sources are outside continuity. Write additive `rows_exact.json` and `readiness_gate_exact.json`; the reader may accept only that exact gate. Do not change sources, embeddings, prompts, question membership or gate thresholds. This repairs the implementation of the already stated sufficient-support requirement; it is not a new success criterion or a further corpus variant.
