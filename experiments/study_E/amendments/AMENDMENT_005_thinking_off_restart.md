# Amendment 005 — thinking-off runtime exploration and full restart

Authorized 2026-09-06 by the user: “I want reader off and test batching. Then restart the run.” In the preceding discussion this means thinking off, with the reader retained. This authorization supersedes the serial-runtime restriction for the bounded test below and permits a full matched restart in this same study. Original registration 2e047c41c80748b16f0e6df15dcc70ac54145598 remains authoritative for population, retrieval, scoring, statistics and dispositions.

## Trigger and scope

The original response exhausted 8,192 tokens; Amendment 004's single repair also exhausted 16,384. Both captures remain immutable and unscored. `reasoning_format=none` leaves reasoning unparsed; the raw empty-think suffix was not a verified thinking-off interface. This is an instrument repair, not a retrieval verdict or an outcome-selected sample change.

## Part 1: bounded exploration before runtime lock

Use the same model and executable hashes, CUDA device, GPU offload, q8_0 K/V, flash attention, no speculation and no context shifting. Use the model's native chat template, server `--reasoning off --reasoning-budget 0`, and `enable_thinking=false` when applying the template. Remove exactly the old trailing `\n<think>\n</think>\n` from the carried prompt; put the remaining instruction, evidence and question unchanged in one user message. Save the rendered prompt and template metadata. Raw completion of that sealed native template preserves the existing capture/scoring format. Compare on/off template renderings and inspect their generation boundaries. No new explanatory instruction, answer grammar or answer constraint.

The common output cap stays 16,384. Each slot has 32,768 context tokens, sufficient only if every rendered input plus output cap fits; verify this mechanically. Benchmark slot counts 1, 2, 4, with total context slots ×32,768 and explicit continuous batching. Cache reuse is disabled for every candidate and the restart to avoid cache-history-dependent prompt evaluation. Other sampling settings and five registered seeds remain unchanged. This changes the reader condition and cannot be pooled with the stopped run or claimed to reproduce Study D.

Select eight development prompts without using correctness: the first C0 and C1 prompt for each of the four before conditions in the first Amendment 003 development group. Run each once at seed 5005 in schedule order, then repeat the identical sequence, at each candidate slot count. Use fixed waves, stable slot assignment, and wait for/persist every wave before the next. Persist every request journal and raw response, including failures; no uncertain-call retry. Record per-response token count, finish state, latency, aggregate throughput and GPU/VRAM samples. Report full distributions and byte identity within repeats and against serial. Allocation failure makes that slot count ineligible; do not silently reduce context or offload.

A candidate is eligible only if all 16 responses finish nonempty at EOS, without a reasoning tag, and both repeats are byte-identical. Batching additionally requires every response byte-identical to serial and at least 10% lower total wall time across the two passes. Pick the fastest eligible candidate, ties favor fewer slots; otherwise retain eligible serial. If serial fails, stop and characterize the specific problem before any new protocol. Gates use completion and identity only, never correctness. A no-tag response can still contain ordinary explanatory prose; this does not prove absence of internal reasoning or answer correctness. Verify actual template control as well as output metadata.

## Runtime lock and restart

Commit exploration evidence and an additive runtime-lock record naming exact template, prompt transformation, selected slot count, command, hashes and measured gates before full inference. Build the entire original 3,840-logical-answer schedule afresh using full newly rendered prompt+seed identities. Keep question/reference/source/vector sets and group/condition/seed/arm order. No previous answer is reused. Full physical aliases may change only because exact rendered bytes differ. Three unscored calibration calls remain: two copies of the largest rendered prompt and the short ready prompt. Their seeded identity is a pre-measurement gate, using the same slots and cache policy as the selected runtime.

For parallel execution assign each consecutive wave to slots 0..n−1 deterministically. Persist per-request uncertain journals before submission and fsync each returned response immediately, not merely at wave end. On a bad response stop issuing waves, drain/preserve already submitted calls, then stop. Preserve raw arrival order and create a separately hashed schedule-ordered archive only at full completeness. A timeout remains uncertain and is never retried automatically. A cap, blank, truncation, reasoning-tag output or missing call blocks scoring. No further cap increase authorized by this amendment.

Verify runner and sealed-input hashes before and after inference. Capture loaded executable/library identities and launch properties. Keep the isolated prior controls pinned and clean. Seal source/prompt/alias/input gates before calling the reader. Commit complete raw archive before blind scoring, and commit all resolved scores before unsealing as already registered. Same reader-only endpoints and guardrails; availability remains diagnostic. Disclose the full restart and exclusions of both stopped captures.

## Preflight

PF1: hash/count the committed development and confirmation inputs, model/executable and all transformed prompts. PF2: save native template on/off renderings and empirical 1/2/4-slot traces. PF3: demonstrate rejection before runtime lock, before input lock, on uncertain journals and incomplete captures. PF4: exercise eligible/ineligible speed, identity and completeness branches with reachable fixtures before selection. PF5: full rendered prompt+seed keys; preserve original logical IDs and question/reference values. PF6: compare unchanged user payloads and source/vector hashes against original sealed artifacts, and require exact seeded replay within and across eligible slot counts. PF7: no response writeback; cache disabled and source/vector hashes rechecked. PF8: development prompts retain 140-record scale; tokenize every confirmation prompt, then calibrate the longest at the actual runtime. The small benchmark does not prove all future calls complete. PF9: throughput/VRAM/no-tag gates cannot certify correctness; runtime identity tests are finite, and the full completion gate still applies. PF10: all four arms and five seeds are regenerated and scored under unchanged reader-correctness criteria.

This design-only amendment precedes implementation. Part 1 artifacts and executed preflight checks must precede runtime selection and the scored restart. No merge, adoption or revised efficacy bar.
